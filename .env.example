# Telegram bot token, get from @BotFather
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
# Telegram admin user id; only this user can control the bot
TELEGRAM_ADMIN_ID=123456789
# Optional: duplicate alerts to a group/channel (leave empty to disable)
# 1) Add bot to the group
# 2) Send any message in the group
# 3) Open https://api.telegram.org/bot<TOKEN>/getUpdates and find "chat":{"id":-100...}
TELEGRAM_ALERT_CHAT_ID=

# Optional exchange API keys for future authenticated features
BINANCE_API_KEY=
BINANCE_API_SECRET=
BYBIT_API_KEY=
BYBIT_API_SECRET=

# Interval in seconds for scan history updates
SCAN_INTERVAL_SECONDS=1

# Default scanner settings
DEFAULT_OI_PERIOD=15
DEFAULT_OI_PERCENT=5.0
DEFAULT_PRICE_PERCENT=1.0
DEFAULT_MIN_OI=100000
SIGNAL_COOLDOWN_SECONDS=60
